#
# Automatic generated configuration for netflow.
# Do not edit this file manually.
#
{% from 'PFWsense/Macros/interface.macro' import physical_interface %}
{%
  if helpers.exists('PFWsense.Netflow.capture.interfaces')
  and
  PFWsense.Netflow.capture.interfaces.strip()
  and
  helpers.exists('PFWsense.Netflow.capture.targets')
  and
  PFWsense.Netflow.capture.targets.strip()
  %}
netflow_interfaces="{% for interface in PFWsense.Netflow.capture.interfaces.split(',')
%}{{
    physical_interface(interface)
}} {%
  endfor%}"
netflow_egress_only="{% if PFWsense.Netflow.capture.egress_only %}{% for interface in PFWsense.Netflow.capture.egress_only.split(',')
%}{{
    physical_interface(interface)
}} {%
  endfor%} {%endif%}"
netflow_version="{%if PFWsense.Netflow.capture.version == 'v9' %}9{% else %}5{%endif%}"
netflow_int_destination="127.0.0.1:2055"
netflow_destinations="{{PFWsense.Netflow.capture.targets.replace(',', ' ')}}"
netflow_active_timeout={{PFWsense.Netflow.activeTimeout|default(1800)}}
netflow_inactive_timeout={{PFWsense.Netflow.inactiveTimeout|default(15)}}
{% endif %}
